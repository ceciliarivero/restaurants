% # encoding: utf-8

<!DOCTYPE html>
  <head>
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/css/layout.css" />
  </head>
  <body>
    <div id="wrapper">
      <div id="header">
          <h1>RESTAURANTS</h1>
      </div>
      <div id="menu">
        <a href="/">Home</a>
        % if session[:user] != nil
          % user_id = session[:user]
            <a href="/edit_user/{{ user_id }}" style="margin-left: 15px;">My account</a>
        % elsif session[:admin] != nil
          % admin_id = session[:admin]
            <a href="/admin/edit_admin/{{ admin_id }}" style="margin-left: 15px;">Admin account</a>
        % else
          <a href="/login" style="margin-left: 15px;">Login</a>
        % end
        % if session[:user]
          <a href="/logout" style="margin-left: 15px;">Log out</a>
        % elsif session[:admin]
          <a href="/admin/logout" style="margin-left: 15px;">Log out</a>
        % else
          <a href="/signup" style="margin-left: 15px;">Sign Up</a>
        % end
      </div>
    <div id="container">
% if defined?(message)
    <p>{{ message }}</p>
% end
{{ content }}
      </div>
    </div>
  </body>
</html>
